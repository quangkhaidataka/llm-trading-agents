# Dev tooling + the minimal runtime needed to import & test the scaffold.
# The heavy runtime stack (langchain, faiss, vectorbt, ...) is in requirements.txt
# and only needed from milestone M1 onward — install it with `make setup-full`.
ruff==0.14.4
mypy==1.18.2
pytest==8.4.2
pydantic==2.12.3
python-dotenv==1.2.1
# Data layer (M1+) needs these to import & test offline (load_prices, read_or_fetch/Parquet,
# compute_indicators). ta is pure-pandas (lightweight), safe for the test venv.
pandas==2.2.3
numpy==2.4.4
pyarrow==23.0.1
ta==0.11.0
# Agent layer (M2+) needs langchain-core offline to build ChatPromptTemplate + the LCEL
# `prompt | ...` chain. Lightweight (no torch/faiss); the heavy langchain-groq backbone
# stays in requirements.txt (online only, function-local import). See ADR-007.
langchain-core==0.3.79
# Memory layer (M3+) uses FAISS for the episode index in BOTH modes (3.4 MB wheel, no
# torch, numpy-2.x OK). The heavy embedding model (sentence-transformers -> torch) is the
# ONLY part mocked offline (deterministic hash embedder); it stays in requirements.txt. ADR-009.
faiss-cpu==1.13.0
